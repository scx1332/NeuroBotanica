{
  "name": "neurobotanica",
  "version": "0.1.0",
  "private": true,
  "description": "NeuroBotanica — Vite/React/TS marketing site with anonymous engagement analytics backend.",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "dev": "concurrently -n client,server -c blue,green \"npm run dev -w client\" \"npm run dev -w server\"",
    "build": "npm run build -w client && npm run build -w server",
    "start": "npm run start -w server",
    "typecheck": "npm run typecheck -w client && npm run typecheck -w server"
  },
  "devDependencies": {
    "concurrently": "^9.1.0"
  }
}
